#include <bits/stdc++.h>
using namespace std;

int main() {
  int a, b, c;
  // order, s[0] is zero
  char s[4];
  scanf("%d%d%d", &a, &b, &c);
  scanf("%s", s[1]);

  printf("%d %d %d\n", a, b, c);
  return 0;
}